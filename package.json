{
  "name": "browsergm20",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "compile": "babel src --out-dir dist && browserify dist/scripts/main.js > dist/scripts/bundle.js",
    "server": "http-server ",
    "build-all": "browserify src/scripts/main.js > dist/bundle.js && babel dist --out-dir dist && http-server"
  },
  "author": "pbaev",
  "license": "",
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/preset-env": "^7.6.0"
  },
  "dependencies": {
    "browserify": "^16.5.0",
    "http-server": "^0.11.2"
  }
}
